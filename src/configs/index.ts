import { app } from './app';
import { mail } from './mail';
import { sendgrid } from './sendgrid';
import { tools } from './tools';

const configs = {
  app,
  tools,
  mail,
  sendgrid,
};
export default configs;
